---
title: "JavaScript ES6+ Cheatsheet"
date: "2024-01-20"
author: "Mr. Natural"
preview: "A comprehensive guide to modern JavaScript features with downloadable cheatsheet."
tags: ["javascript", "tutorial", "reference"]
emoji: "🔥"
---

# JavaScript ES6+ Cheatsheet

JavaScript has evolved significantly with ES6 and beyond. This post covers the most important modern JavaScript features you should know.

## Arrow Functions

Arrow functions provide a shorter syntax for writing function expressions:

```javascript
// Traditional function
function add(a, b) {
  return a + b;
}

// Arrow function
const add = (a, b) => a + b;

// With block body
const multiply = (a, b) => {
  const result = a * b;
  return result;
};
```

## Destructuring

Destructuring allows you to extract values from arrays or properties from objects:

```javascript
// Array destructuring
const [first, second, ...rest] = [1, 2, 3, 4, 5];

// Object destructuring
const { name, age, ...others } = {
  name: "John",
  age: 30,
  city: "NYC",
  country: "USA",
};
```

## Template Literals

Template literals make string interpolation much easier:

```javascript
const name = "World";
const greeting = `Hello, ${name}!`;

// Multi-line strings
const html = `
  <div>
    <h1>${greeting}</h1>
    <p>Welcome to JavaScript!</p>
  </div>
`;
```

## Spread Operator

The spread operator (`...`) expands iterables into individual elements:

```javascript
// Array spreading
const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];
const combined = [...arr1, ...arr2];

// Object spreading
const obj1 = { a: 1, b: 2 };
const obj2 = { c: 3, d: 4 };
const merged = { ...obj1, ...obj2 };
```

## Async/Await

Async/await makes asynchronous code look and behave more like synchronous code:

```javascript
// Promise-based approach
function fetchData() {
  return fetch("https://api.example.com/data")
    .then((response) => response.json())
    .then((data) => console.log(data))
    .catch((error) => console.error(error));
}

// Async/await approach
async function fetchData() {
  try {
    const response = await fetch("https://api.example.com/data");
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.error(error);
  }
}
```

## Modules

ES6 modules allow you to organize your code into reusable pieces:

```javascript
// export.js
export const PI = 3.14159;
export function square(x) {
  return x * x;
}
export default class Calculator {
  add(a, b) {
    return a + b;
  }
}

// import.js
import Calculator, { PI, square } from "./export.js";
```

## Conclusion

These are just some of the powerful features introduced in modern JavaScript. Mastering them will make you a more productive developer!

For a complete reference, check out the MDN documentation[^1].

[^1]: [MDN Web Docs - JavaScript](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
